#include <iostream>
using namespace std;
int main10 ()
{
	int a;
	cout<<"nhap vao 1 so nguyen : ";
	cin>>a;
	if(a%2==0)
		cout<<"so " << a <<" la so chan"<<endl;
	else
		cout<<"so " << a <<" la so le"<<endl;
	 system("pause");
	return 0; 
}