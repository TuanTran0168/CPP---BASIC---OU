#include <iostream>
using namespace std;
int main11 ()
{
	int a;
	cout<<"nhap vao 1 so nguyen : ";
	cin>>a;
		if(a==0)
		cout<<"so "<< a <<" la so 0 "<<endl;
		if(a>0)
		cout<<"so "<< a <<" la so duong"<<endl;
	else
		if(a!=0)
		cout<<"so "<< a <<" la so am"<<endl;
	 system("pause");
	return 0; 
}